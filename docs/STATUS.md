# Current implementation and deployment status

## Actual checkout snapshot at every process boot — implementation in progress (2026-09-11)

- Root explicitly changed `!version` semantics: snapshot the development checkout's actual HEAD, branch and dirty state at every process boot, including documentation/empty commits and branch switches; freeze until the next boot. This is deliberately not the image's source revision. Keep image metadata/OCI revision intact for separate operational diagnosis; reporting checkout state does not reload edited code into an immutable image.
- Existing constructor capture is retained. New configured AF_UNIX reader path is consulted once per process, with a total deadline, bounded/validated JSON and no baked-metadata fallback. A configured reader failure refuses startup rather than reporting another checkout/image as truth. Source-only runs without any Git source report unknown. The primary command label becomes `Checkout at boot`.
- Read-only service-identity probe was denied at `/home/codexy` (0700); some tracked sources are0600. Do not grant home traversal or mount the private working tree/`.env` into Curie. A per-instance systemd socket-activated reader runs briefly as checkout owner, computes fixed-repository Git state, emits only commit/branch/date/subject/dirty, then exits. Only its stable socket directory is mounted read-only into the bot, never API or generated services. No public port, resident watcher or new Docker helper kind. Reader/units are root-installed, socket is0600 for the service identity, parent remains root-owned.
- Candidate code and synthetic Git/socket/report/unit regressions are written; guarded source tests, real activation/restart acceptance and deployment remain pending. **Live is still `maxwell-app:c010a37`, with the previous image-based version fallback.** No private configuration, primary provider settings or runtime memory has been changed for this work.

## Numeric OpenRouter effort and private operator commands — deployed (2026-09-11)

- Root explicitly rejected the local three-preset restriction and authorized sending every integer1–100 to the configured DeepSeek **V4.1 Flash** OpenRouter route, regardless of advertised enum schemas. `!effort N` now persists the integer and emits `reasoning.effort: N` unchanged, including50/75/100; no rounding or named-tier substitution. Existing named reasoning commands, direct-API behavior, per-call disable overrides, other models/routing/credentials remain unchanged. No live setting has been changed by this implementation.
- Guarded candidate `5121ccf683dc60c8dd7dd430b249acc0ab9276fe` passed **655 focused tests**, zero failures/errors/skips, 4.534s. Coverage includes all100 integers through real command dispatch/persistence/reload and provider payload construction, integer wire types, invalid input/no mutation, existing direct-API semantics, and full simulated OpenRouter400 rejection bodies plus exact integer request metadata under the production incident handler, without tier retry. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/5121ccf683dc-vgqxsrz0/`. This proves local behavior, not upstream acceptance or that OpenRouter honors the value.
- Numeric source committed as `f0b5468`. Its first full run was **3,186 passed / 1 failed** in the existing slotted-message site-cache identity-reuse test (`tests/test_site_tools.py::test_site_guards_work_on_slotted_discord_messages`), not numeric effort. No test was removed or weakened. Later complete acceptance below passed with that test included.
- Added exact-path, two-artifact interpreter-link backup compatibility: raw bytes retained, derived strict-valid tar plus mutually bound private metadata, complete graph/identity checks, deferred no-follow reconstruction through the existing rootless helper. Generic archive validation/data-filter rules remain unchanged. Independent review found parent-directory timestamp loss during deferred creation; fixed and covered by a real roundtrip regression.
- Actual rootless acceptance then uncovered an **existing standard restore descriptor bug**: after buffered tar validation, Python's logical offset was0 but the inherited kernel descriptor remained30720, so the child received EOF. `restore` now opens archive input unbuffered; three real-subprocess descriptor regressions cover small/large inputs. This fixes transport, not validation or restore permissions. The resulting candidate `fe189f04e76c1ca522c33bea5cf4a82a26f099be` passed **745 focused tests** (4.079s) and **3,284 complete tests** (101.788s), zero failures/errors/skips. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/fe189f04e76c-gdq4vj8f/` and `fe189f04e76c-rnh9the3/`.
- **Actual disposable rootless recovery passed** using the same helper command/capabilities, `--network none`, no private mounts/login, existing validated Python3.14.4 image and synthetic UID/GID10001 files. Proved raw unchanged/strict raw rejection, pair validation, real standard restore followed by deferred reconstruction, exact SQLite/file bytes, hardlinks/relative links, link/parent ownership and mtimes, and existing-leaf refusal. Evidence: `/home/codexy/.cache/maxwell-errors.SlSdzk/compat-acceptance-passed.json`, synthetic state `/tmp/maxwell-compat-proof-rbd8kc_b`. This is not yet a fresh production backup.
- **Live application: `maxwell-app:c010a37`**, source `c010a37d794add6f22da0d53c7a2226526feee1f`, image ID `sha256:77ceccccafa7d1a66b2171e84b7baec90a28df979bbb51eb2530599f226ab9ec`; web remains `maxwell-web:01e8cbb`. The exact committed release passed **3,284 complete isolated tests**, zero failures/errors/skips,101.893s: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/c010a37d794a-nmzieqp_/`. Actual built-image constructor/dispatcher acceptance passed all100 numeric commands, persistence and integer payloads, private incident retrieval/redaction/attachments/ingress exclusion, generic no-footer failures and concurrent own-only cleanup preserving synthetic stored data/cache. Evidence: `/home/codexy/.cache/maxwell-errors.SlSdzk/image-acceptance-c010a37.log`.
- **Cutover completed:** writers stopped04:10:47UTC, fresh cold snapshot04:10:49, source/image installed04:10:52, stack plus prior managed services started by04:11:46. Fresh raw `/srv/maxwell-backups/curie/pre-incidents-c010a37.tar` is preserved; complete recovery requires its strict `pre-incidents-c010a37-compat.tar`, companion `pre-incidents-c010a37-compat-link.json`, and tested `/srv/maxwell-backups/curie/recovery-c010a37/` code. Pair/identity/full-graph validation passed before installation. Private prior source/config/control, hashes and manifest: `/srv/maxwell-rollback/curie/incidents-c010a37/`. Retained previous image `36e5eab` and managed image IDs; no data backup was restored and no live symlink was deleted or repointed.
- **Live acceptance passed:** one unchanged fresh online Discord identity; bot/API exact expected source/image and zero restart counts; API/web/Ollama healthy; provider initialized; both local embedding probes passed; all691 prior embedding hashes and794 prior stored-content hashes preserved; private config/control and prompts unchanged; both previously running managed containers restored; dashboard loopback8081/authentication maintained; incident exports denied to both unauthenticated and authenticated requests; zero current-epoch error lines. No test Discord messages or chat-provider completions were sent. The first health helper used the wrong `/api/data/...` URL; corrected to the existing `/data/...` route and reran successfully, without changing runtime routing.
- Refreshed only the stale Compose log follower in existing attached `152732.dame_curie` window0 after cutover; no extra bot restart, no detached display or replacement Screen. Verified the new follower and fresh visible Ollama logs through04:24UTC (Screen uses slash-formatted dates there). Live numeric HTTP acceptance/model behavior is still for root's real requests to establish; no silent fallback to a tier is implemented. `!error 0..9` and `!forward N` are now live alongside numeric `!effort 1..100`.

## Initial incident rollout — validated, first cutover deferred (2026-09-11; superseded above)

- Root authorized checkpoint, implementation, validation and restart. Explicit rollback checkpoint: `cfc6c05292633c45f4cbd4940e3cf46776358537` (clean source at `0862711`; no runtime changes). **At this initial checkpoint, live remained `maxwell-app:36e5eab`; the later completed cutover is recorded above.** No credentials/model/persona/routing/settings changes are authorized as part of it.
- Requested public automatic error notice: `I waited, waited and I am losing things like tears in the rain 🕊️`, without a measurement footer. Root chose **always DM** for admin `!error 0` through `!error 9`, even when invoked in a server. Index is mandatory; 0 is newest, 9 oldest. Reports are global to this identity, not scoped to the command's origin. Normal validation/refusal/cancellation is not a new runtime incident.
- Implemented the private incident-store/provider foundation: global last-ten JSON history, private mode0600 atomic/flock persistence, full exception chains and received upstream error text, request parameters/shape and correlation metadata, registered-credential/auth-material redaction, immutable retrieval snapshots, and same-exception/request/log-record deduplication. Failed retries are grouped per provider request. Ordinary logs remain accessible; no raw prompt transcript, Authorization/Cookie request headers or environment dump is added to reports. Received SSE data is retained on failure; unread transport bytes are not drained. Corrupt/unwritable history is reported as unavailable without overwriting corruption; there is no in-memory persistence fallback.
- Foundation candidate `407b23a5a68351f4319e8aa207862ea92f33904c` passed **406 focused tests**, zero failures/errors/skips, 2.923s, in the guarded Python3.14.4 source-only/disabled-dotenv/private-read/network-isolated runner. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/407b23a5a683-_vzqhn0n/`. Includes installed-production-handler retry grouping, provider fallback/streaming/timeout behavior, full error-body tails, secret redaction, file/concurrency safety and log preservation. An earlier same-LogRecord propagation double-capture was fixed without weakening its test. Later integration and complete source/image acceptance results are recorded below.
- Root additionally confirmed `!forward N` must delete only Curie's own last N Discord messages in the same channel/DM, before the invocation; never another author or the invocation. It must be deterministic/admin-only, serialize selection plus deletion, and remain silent on success. **All existing stored context, memory, embeddings, REM and tool history are sacred and must remain untouched.** The existing model-accessible `DeleteMessageTool` and broader purge capability remain available; the new command must not invoke them.
- Foundation committed as `24d153e`. Integrated deterministic private `!error`, serialized own-only `!forward` (including partial-batch diagnostics and plugin suppression), generic foreground/Telegram/job/tool failure notices, full provider/tool/backend capture, API-private shared storage, and preserved model tool/log feedback. No runtime deployment yet. Reports and usage/index/empty-history answers are DM-only; if DM delivery fails, only the generic dove may be sent at the origin, never a report/file fallback.
- Cancellation-retention follow-up candidate `3bf949a7755a47d88be3addf2c228188dee1321e` passed **417 focused core/provider tests**, zero failures/errors/skips, 2.932s. It preserves already-received failures across cancellation (including cancellation inside retry exception handlers), distinguishes inherited caller cancellation, records known non200 status before body reads without claiming unread data, and excludes pure cancellation while retaining genuine cleanup exception chains. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/3bf949a7755a-kxf7t_8o/`.
- First guarded integration candidate `ca766cb6aed1b3396a22bbd32687ed4d077abd9f` passed **372 tests with one test-expectation failure**, no errors/skips, 29.389s. The failing test incorrectly expected separate incidents for a DM failure and its nested notice failure; both are deliberately grouped into one exception-chain incident. The corrected assertion now requires both complete traces in that same incident and exactly one generic notice attempt. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/ca766cb6aed1-e1615u5i/`. This is not yet a passing full-suite result.
- Independent source review found additional real edge cases: diagnostic-spool I/O must not disable shell/log tools or skip process/pump/heartbeat cleanup; ordinary backend env values must not poison global redaction; cancellation after a received upstream error must retain that prior failure without recording ordinary cancellation; intentional container-mode PM2 501 refusals must not evict incidents; remaining invite/member/channel failures need full capture; global CAPTCHA DMs need the same private-ingress marker. The fixes and failure-injection regressions are implemented. Refreshed integration candidate `78a3ae808c6bf5331190f1ec35b6b1e6312f5516` passed **448 focused tests**, zero failures/errors/skips, 32.373s, including shell-spool fault/cancellation cleanup, private command delivery, full multipart reports with explicitly closed buffers, real source-curated constructor, and Docker allowlists for the new modules. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/78a3ae808c6b-6e2gd_ad/`. Independent re-review closed the original findings and caught one final retention-order gap: a real pipe/timeout/nonzero-exit failure must be recorded before cancellation arriving during cleanup is propagated. That narrow correction passed **205 focused tool/shell/operator/job regressions**, zero failures/errors/skips, 2.574s, candidate `1743140ca12e01102137613095e3e5ad9a348a31`, evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/1743140ca12e-0hb2vvbe/`. Cleanup-induced kill statuses remain nonincidents and successful tool results stay successful. The subsequent complete source and actual-image acceptance both passed, as recorded below.
- Root explicitly excluded **REM/autonomy audit and status display changes** because they are not currently used. Their existing outputs and stored data remain unchanged; mixed legacy audit text is not claimed safe. Actual runtime-failure notices remain generic. Other automatic projections (job tool summaries/progress, shell stderr, guide/plugin failures) are closed in the validated candidate while preserving model-facing diagnostics. Root accepts that a model with retained log access can deliberately quote diagnostics; this is not a generated-text filter and does not alter generated-site response bodies.
- Committed application source `e306a220df033b0f22354a441624e3fa7450e35a` passed the **complete 2,839-test suite**, zero failures/errors/skips, 99.218s, under guarded Python3.14.4 with the mandatory live-credential test excluded. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/e306a220df03-6ilhemyp/`. Built source-frozen `maxwell-app:e306a22`, image ID `sha256:a8ffd373132b86382ff48af3ca19cf61d3cf23047d2fda0cb326939ac12b2b40`, from a Git archive through the explicit source allowlist. Actual-image synthetic acceptance **passed**, exercising the real constructor and dispatcher, real bot-admin check with synthetic admins, global rotation/index/DM-only retrieval, complete redacted multipart attachments, report/plugin ingress isolation, generic no-footer failures, concurrent/repeated own-only silent cleanup with unchanged stored data/cache, retained model delete/purge tools and existing reasoning/attribution. Container used network none, synthetic tmpfs state and mocked Discord, with no private mounts/login/provider completions. The retained prior `maxwell-app:36e5eab` also passed a fresh network-none synthetic constructor/command rollback probe, without private state or login. The attempted cutover subsequently stopped at cold-backup validation, before installing source or changing the image; see the current outcome below. Build/acceptance/lifecycle helpers and sanitized evidence: `/home/codexy/.cache/maxwell-errors.SlSdzk/`.
- **Cutover deferred:** writers stopped at02:59:42UTC; a cold archive was created, but standard `validate_archive` rejected one existing shell-workspace `python3` symlink targeting `/usr/bin/python3` outside its state root. No new source/image was installed and no live files/links were removed, rewritten or permission-relaxed. Preserve `/srv/maxwell-backups/curie/pre-incidents-e306a22.tar` as a raw snapshot, **not a standard-restorer-validated backup**; private prior source/config/control/manifest and link metadata are in `/srv/maxwell-rollback/curie/incidents-e306a22/`. Do not rerun the one-shot deploy helper unchanged: those paths already exist, and RAG fingerprints/install/start phases were not reached. Prior stack and previously running managed containers were resumed; bot started at03:03:29UTC on `maxwell-app:36e5eab`. Read-only03:13UTC inspection confirmed running state, the same identity and a fresh post-restart snapshot. At that point the rollout goal was paused while root investigated effort semantics; compatibility and the subsequent cutover were completed later as recorded above.
- **Effort investigation (03:13UTC):** configured model remains `deepseek/deepseek-v4.1-flash` with `provider.only=["deepseek"]`. Saved primary reasoning and configured baseline are both `high` (the bot's75 reference preset), also `high` in the pre-stop snapshot. Logs show control reloads at01:11:29/34/48UTC and03:03:33UTC; message-body logging is disabled, so exact recent command arguments cannot be recovered from console logs. No reasoning/effort command exception was found in the inspected interval. In that old live build both command aliases wrote the same `deepseek_reasoning` field; only exact50/75/100 numeric presets were accepted locally. No effort setting was changed during investigation. V4.1-specific [reference encoding](https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/blob/main/encoding/README.md) explicitly supports integer1–100 and aliases low=50/high=75/max=100, with a separate thinking/chat toggle; the [current hosted API schema](https://api-docs.deepseek.com/api/create-chat-completion) documents string tiers. Full integer passthrough through the configured OpenRouter route remains unverified; model support and hosted API exposure must not be conflated.
- The prior HTTP400 diagnosis remains unproven because its response bodies were discarded. Native reasoning state preservation/per-turn effort stability and own-footer model visibility are separate follow-ups, not silently repaired here. Existing discarded bodies cannot be recovered, and the new store does not import old console logs.

## Discord command readability — deployed (2026-09-10)

- **Live application: `maxwell-app:36e5eab`**, source `36e5eab4c80c6d7589b9525dbac64a67d0b30b1e`, image ID `sha256:69fcae4c27dd9b12f74e4c142c16906c4d53c77666ef66a0fdea5349853d3cf4`; web remains `maxwell-web:01e8cbb`. Root requested fenced code blocks for dense Discord command reports, a permanent documentation rule, and deployment/restart. `AGENTS.md` now requires plain triple-backtick fences, safe per-message splitting and emitted-content tests; terse replies, deliberate rich help and clickable links stay normal.
- Reasoning/effort reports, footer status, X/VC status, REM/autonomy status, context listings, REM audits and autonomy logs now reuse `send_command_response(..., code_block=True)`. VC settings are one field per line rather than a dense bold pipe-separated sentence. No command/control/provider behavior changes; no new placeholder footers on previously raw reports. Short denial/error/empty results remain plain. Existing debug/version fences and rich help are preserved.
- **143 focused tests passed**; exact source `36e5eab4c80c6d7589b9525dbac64a67d0b30b1e` then passed the **full 2,516-test suite**, zero failures/errors/skips, 96.756s, in the guarded Python3.14.4 source-only environment with the mandatory credential-reading test excluded. Tests inspect actual emitted fence boundaries, embedded-backtick neutralization, chunks within 2,000 characters, report footer behavior and unchanged short replies. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/eca97e960f39-tj9pwlpj/` (focused), `36e5eab4c80c-x7zi8ebr/` (full).
- The first full run (`36e5eab4c80c-q4i0qlrd/`) had 2,515 passes and one failure in unchanged `test_site_guards_work_on_slotted_discord_messages`. Its module passed all 27 tests on prior source `b527ee9`. An isolated execution of that baseline's exact `_site_turn_state` function reproduced the actual defect: a new slotted object reusing a freed object's `id()` inherits its cached guard counter. The unmodified candidate's full rerun passed; no tests were newly excluded or assertions relaxed. **Separate existing follow-up:** fix lifetime/identity tracking in `_SITE_TURN_STATE`; not part of this formatting patch.
- Built-image acceptance passed using the real constructor with synthetic tmpfs state, no network/private mounts/real credentials, mocked Discord sends and mocked context retrieval. It verifies actual emitted reasoning/effort/footer fences, long context splitting with embedded fences, plain denial and unchanged reasoning/routing/attribution. Image/helper evidence: `/home/codexy/.cache/maxwell-formatting.IVtcmS/`. Independent read-only review found no introduced blocker.
- Standard fresh backup and archive validation succeeded under the instance operation lock; no editor recovery files or permission bypass this time. Writers stopped at23:55:56UTC and the stack plus both managed containers resumed at23:56:55UTC. Backup: `/srv/maxwell-backups/curie/pre-formatting-36e5eab.tar`; private config/control/source/manifest rollback: `/srv/maxwell-rollback/curie/formatting-36e5eab/`; prior working image `36122a4` retained. Only the application image/source changed: private `bot.env` is byte-for-byte unchanged, controls and prompts unchanged, all 765 pre-maintenance embedding hashes preserved. No key rotation, provider/effort/image-setting change, live state restore or dashboard UI change.
- Live acceptance verified the exact running `bot.py` against the tested Git archive, one unchanged fresh online Discord identity, initialized provider, zero bot/API restarts, healthy API/web/Ollama, both embedding probes, restored managed site/shell, retained attached `dame_curie` Screen and zero bot-epoch error lines. Dashboard remains authenticated/loopback8081, with private configuration inaccessible. No test Discord messages or chat completions were sent; output-format acceptance uses actual emitted payloads with mocked sends, not a claim of inspecting Discord's client UI.

## Reasoning, native images and OpenRouter usage — deployed (2026-09-10)

- **Live application: `maxwell-app:36122a4`**, frozen source `36122a48e240baa61d40813dd0a0aebfa436eec7`, image ID `sha256:46abbd7482c4d45527e92002866c36d5bdf8c3e038c4d74df9d08fb1aa5c878e`; web remains `maxwell-web:01e8cbb`. Root authorized rollout/restarts. The same Discord identity is online once; bot/API use read-only roots, zero restart counts, healthy API/web/Ollama and successful provider initialization. Both previously running managed containers (site and shell) are restored. Existing attached `152732.dame_curie` Screen session is retained.
- Discord `!reasoning` reports the loaded primary setting; administrators can set `low/high/max/off`. `!effort` reports or sets exact reference presets `50/75/100`. Settings persist and affect subsequent primary calls without restart. **Arbitrary numeric 1–100 hosted API control is not implemented because support is unverified.** The configured baseline is now explicitly `reasoning={"enabled":true,"effort":"high"}`; auxiliary disable calls explicitly emit off. Current verified hosted model permits off. The chat model, official-DeepSeek `provider.only` pin, OpenRouter key, attribution headers, other settings and prompts are preserved. No primary model/provider/key switch or dashboard UI change.
- Dedicated image keys were copied privately from `/opt/cliproxyapi/config.yaml` into the two profiles, never into source/build/test artifacts. Normal: `images`, `http://192.168.241.2:8317/v1`, `gpt-image-2.5-flare`, `quality=low`, timeout300s. HD: same private base/protocol, `gpt-image-2.5-sunburst`, `quality=max`, timeout600s; native edits preserve reference bytes. Both profile credentials authenticate from the running bot. **The local proxy ignores the requested quality distinction in observed tests; true higher-quality/slower HD, selected resolution or actual model identity are not demonstrated.** The chosen model/quality fields are explicit, not a claim that the gateway honors them. See [configuration details](CONFIGURATION.md#independent-normal-and-hd-image-configuration).
- `usage` is safely re-enabled after exercising the **actual new tool** from the running image against OpenRouter `/api/v1/key`. It reports per-key USD spending/caps and separate BYOK accounting, using the loaded primary key and a fixed HTTPS origin with redirects disabled. It sends nothing for unsupported primary providers. No management/workspace key is needed; no z3ki URL override, raw response, key label or identifier is returned. Totals include all callers sharing this key, not necessarily only Curie. Root plans rotation after task; the existing chat key remains unchanged.
- Exact-source Python3.14.4 full suite: **2,495 passed**, zero failures/errors/skips, 95.766s; mandatory live-credential test excluded. Source-only checkout, synthetic home/data/sites, disabled dotenv, private-read barrier and isolated network. Evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/36122a48e240-0vt1ew24/{pytest.log,results.xml}`. Actual built-image acceptance (network none, no private mounts/real credentials) exercised the real bot constructor, Discord command dispatch/persistence, reasoning payloads, per-key usage and both native image profiles with mocked transports/delivery. Independent read-only review found no introduced blocker for the documented configuration.
- Maintenance first stopped writers at21:29:57UTC; backup stopped on an unreadable **root-owned Vim swap file** before any source/image/config replacement. Root closed the editor; its saved settings were unchanged. The standard backup then succeeded and was validated, source/private settings were installed atomically, and services resumed at21:44:15UTC under the instance operation lock. No swap was deleted or permission boundary weakened. Fresh state backup: `/srv/maxwell-backups/curie/pre-capabilities-36122a4.tar`; root-only rollback config/control/source/manifest: `/srv/maxwell-rollback/curie/capabilities-36122a4/`; previous known-working app `677d2e4` retained. A rollback must retain `usage` disabled to avoid restoring the old unrelated-key forwarding. No backup restore was performed against live state.
- Live read-only checks verified new request fields and actual key usage, both image-key model listings, same fresh identity, both embedding probes, **all 651 pre-maintenance stored embedding hashes**, unchanged prompts/other settings, authenticated dashboard, unauthenticated API/private-config denial, and zero bot-epoch error lines. Dashboard remains loopback8081 and proxy publication loopback8317; the proxy's internal bridge attachment survives restart but must be restored on proxy-container recreation. No Discord test messages or chat test completions were sent. The only live generation acceptance was two synthetic local-proxy images; helpers/evidence: `/home/codexy/.cache/maxwell-capabilities.rqyL88/`.
- Separate pre-existing follow-up found during review: local HD reference path checks use lexical rather than resolved paths and do not confine symlink targets. This was not introduced or modified by the native adapter; do not claim realpath confinement. The documented literal proxy address is required with the current shared resolver; private DNS hostnames are not supported by that resolver.

## Pre-deployment capability development and usage containment (2026-09-10)

- Root confirmed the unintended `usage` request to the author's `z3ki.dev/v2/usage` endpoint and approved containment. The tool was disabled through the authenticated control API, preserving every other control; no restart required. Prior control file: `/srv/maxwell-rollback/curie/usage-disable-20260910T211032Z/`. The existing adapter borrows the primary chat key and parses pooled-account quota percentages, not OpenRouter token/spend/cache statistics. Root subsequently requested replacement with OpenRouter's per-key reporting; keep the old implementation disabled until safe code is deployed. Root plans key rotation after this task; no credential rotation was performed.
- Current sanitized configuration: primary `deepseek/deepseek-v4.1-flash` at OpenRouter, `provider.only=["deepseek"]`, reasoning-disable false and no explicit reasoning fields. Normal image tool uses Pollinations `flux`; HD dedicated base/key are unset. These are current observations, superseding older model snapshots below. Existing attribution headers remain configured.
- Priority reasoning source adds Discord `reasoning`/`effort` reporting and admin updates, persisted primary overrides and explicit hosted request parameters. Current OpenRouter model metadata advertises reasoning enabled by default, default `high`, supported `low/high/max`, and `mandatory=false`; official direct API also documents disabling. The reference encoder's numeric presets are 50/75/100. Arbitrary 1–100 hosted passthrough is not verified and is not fabricated. **398 focused isolated tests passed**, then **2,322 complete isolated tests passed**, zero failures/errors/skips, unsafe credential test excluded; source not yet deployed. Evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/reasoning-worktree-gd3tls26/` (focused) and `reasoning-worktree-8cio_if4/` (full).
- Native image integration source is ready: independent normal/HD credentials, native generation and JSON edits, one non-redirecting POST, original native reference bytes and preserved delivery/preview behavior. **199 focused isolated tests passed**; evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/a2c9907eed1a-woqv7a_6/`. OpenRouter usage replacement source uses the loaded primary host/key pair, fixed trusted HTTPS `/api/v1/key`, redirects disabled and whitelisted USD/accounting output without raw response/secret disclosure; unsupported providers make no request. **64 focused isolated tests passed**; evidence `openrouter-usage-worktree-af9r4qa8/` in the same cache root. A read-only live key query succeeded with the existing ordinary key; no management credential is required. Both integrations await combined source/image acceptance and deployment.
- Updated local proxy is `eceasy/cli-proxy-api:v7.2.157`; model listing includes GPT Image 2.5 Flare/Sunburst. Safe synthetic round trips requested Flare/low and Sunburst/max successfully (13.94s/13.14s), but both responses reported `quality=low`; maximum-quality upstream execution is **not proven**. Root's requested maximum settings will still be explicit. Proxy-only internal bridge `maxwell-curie-cpa`, `192.168.241.0/29`, adds proxy address `192.168.241.2:8317`; actual Curie reachability returns unauthenticated401. No public listener, proxy restart, shared Docker socket or bot identity change. This avoids overlapping rootful/rootless `172.17.0.0/16` networks without allowing host-loopback access. Recreated proxy containers must be reattached to this bridge before image use.

## OpenRouter app attribution — configured and restarted (2026-09-10)

- Root supplied `https://council.zombiedawn.net/` and authorized restart. Private `/srv/maxwell/curie/config/bot.env` now sets `OLLAMA_EXTRA_HEADERS='{"HTTP-Referer":"https://council.zombiedawn.net/","X-OpenRouter-Title":"Dame Curie: Always Teasing"}'`. Attribution uses HTTP headers, not body parameters; the URL identifies the app and the title names it in OpenRouter analytics. See [OpenRouter's attribution contract](https://openrouter.ai/docs/app-attribution) and the [configuration guide](CONFIGURATION.md#custom-request-options-and-openrouter-routing).
- **Configuration-only: no application code/image change.** Every other parsed setting, including custom body/routing, model, persona and credentials, is unchanged; private configuration owner/mode are preserved. Byte-verified pre-change config and sanitized identity/container baseline are retained under root-only `/srv/maxwell-rollback/curie/attribution-20260910T175118Z/`. Configuration replacement held the instance operation lock and was atomic.
- The installed operator restarted **only bot/API once**, under its operation lock. App remains `maxwell-app:677d2e4`, web `maxwell-web:01e8cbb`; existing container IDs/images and dependency startup epochs are unchanged. One same Discord identity, fresh online snapshot, successful provider initialization, zero bot-epoch error lines, healthy API/web/Ollama and expected header parsing in both containers verified. Authenticated dashboard succeeds, unauthenticated API/private config remain denied, and publication remains loopback-only.
- Exact checkout source `7694c1073c624ed36da49e8f0e3cd908eaa4c0ae`: **91 focused provider/configuration tests passed**, then **2,139 complete offline tests passed**, zero failures/errors/skips, mandatory credential-reading test excluded. Python 3.14.4, source-only snapshots, clean synthetic roots, disabled dotenv, private-read barrier and loopback-only network namespace. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/7694c1073c62-bvecukdz/` (focused), `7694c1073c62-1mkl1ckg/` (full).
- Disposable acceptance using the **actual deployed image**, no network/private mounts/credentials and a mocked completion transport, verified that the exact title/URL reach primary POST headers rather than the body, with routing and Authorization preserved and fallback untouched. Helpers: `/home/codexy/.cache/maxwell-attribution.aS4WWz/`. **No paid test completion or Discord test message was sent; OpenRouter dashboard display itself was not observed.** Attribution applies to subsequent primary-client requests under the existing scope, not fallback/vision/separate clients or historical usage.

## Live image/debug repair rollout (2026-09-10)

- **Live app: `maxwell-app:677d2e4`**, frozen source `677d2e49c555f5a64bf904ed74a6bfbba9f9bc67`; web remains `maxwell-web:01e8cbb`. Authorized maintenance ran 03:05:10–03:06:07 UTC under the private instance operation lock: quiesce writers, fresh validated state backup, install tested source/image reference, `start`, resume previously running managed containers. One real Discord identity, no parallel login. The existing Screen window 0 remains available.
- **All private configuration and prompt files are byte-for-byte unchanged from the fresh backup.** Root's configuration at rollout is `google/gemini-3.7-flash` on `openrouter.ai`; this is the user's current setting, not an agent model/provider change. Dashboard credentials and isolation are unchanged. `start` remains an exact `up` alias; `restart` remains bot/API-only.
- HD availability restored through the authenticated control API after live acceptance, changing only its disabled-tools entry. **Dedicated image base/key are currently blank**: HD now returns a configuration error without borrowing chat settings or submitting generation. Set `GEMINI_IMAGE_*` explicitly when choosing a compatible image gateway. Pollinations remains available. One generation submission per invocation, canonical returned-image parsing, same-turn duplicate-preview suppression and loaded-runtime/fenced `!debug` are deployed.
- Acceptance: bot/API use the new image with read-only application roots and zero restart counts; API/Ollama/web healthy, fresh online snapshot with the same Discord user, actual embedding checks pass from both bot/API. Authenticated dashboard succeeds; unauthenticated API and private config exposure are denied; only loopback8081 is published. Current bot epoch had zero error-level lines and zero DNS failures. No paid image test or Discord test message was sent.
- All **922 pre-maintenance stored embeddings** retain their exact hashes. Subsequent read-only status: 996 total rows, 961 embedded, **35 eligible pending**, zero excluded/stale/invalid. New embeddings are progressing; no historical backfill was performed. Raw memory was preserved, not silently re-embedded or relabeled.
- Fresh state backup: `/srv/maxwell-backups/curie/pre-images-677d2e4.tar`. Root-only rollback material: `/srv/maxwell-rollback/curie/images-677d2e4/` (current pre-rollout configuration/control, operational source and integrity manifest). Prior app `99fd7fa` is retained. A rollback to old image code must first disable HD again to avoid restoring its implicit billing fallback/retry bugs; do not restore older, stale chat configuration.
- Exact-source full suite: **2,139 passed**, zero failures/errors/skips, mandatory unsafe test excluded. Source-only build plus actual-image offline constructor/behavior acceptance and live health evidence are documented below; rollout helpers/evidence are under `/home/codexy/.cache/maxwell-image-repair.BcJpcl/`.

## Release acceptance before deployment (2026-09-10)

- Final application source **`677d2e49c555f5a64bf904ed74a6bfbba9f9bc67`** passed the complete isolated Python3.14.4 suite: **2,139 passed, zero failures/errors/skips**, 95.041s, mandatory unsafe test excluded. Evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/677d2e49c555-wouibxq_/{pytest.log,results.xml}`.
- Built **`maxwell-app:677d2e4`**, image ID `sha256:4d718cad7e19f4ec84c4dbd3a7de71dbe09e8360221dee55ad58e20bf3d14b14`, from that Git archive only with frozen build metadata. Disposable acceptance used the actual image, no network/private mounts/real credentials, read-only application and private tmpfs state. Real bot construction/close passed; loaded debug worked before inference; mocked canonical image response uploaded once, empty response submitted once, keyless image config borrowed no chat key, and signed/queryless link suppression preserved labels/other previews. Evidence `/home/codexy/.cache/maxwell-image-repair.BcJpcl/{build.log,acceptance.py,acceptance.log}`.
- Root approved rollout after these checks. Fresh rollback capture and controlled maintenance are next; production is still `99fd7fa` until the rollout entry below is superseded.

## Duplicate image preview — implementation checkpoint (2026-09-10)

- Foreground Discord delivery now suppresses previews only for image links already delivered by successful image tools in that response's local `all_tool_results`. Inline/Markdown links remain clickable; other URLs, code spans, later turns and concurrent channels are unchanged. Discord attachment links match by host/path despite removed/changed signed queries; other resource URLs require exact matches. No second attachment existed in the final text path: this is targeted unfurl suppression, not message-wide `suppress_embeds` or a generation/delivery retry change.
- The existing visible-reply sanitizer also stripped Markdown labels. One bounded bracket-regex change now preserves inline labels while retaining protocol-marker/envelope removal; it cannot backtrack across an earlier closing bracket. Original response history/REM and response-owned measurement/footer handling remain separate from formatted delivery.
- **199 focused isolated tests passed**, including 43 new formatter, actual foreground-path and sanitizer cases; one mandatory unsafe test deselected. A baseline-bot negative control reproduces 10 failures. Python3.14.4, source-only/clean env/private-read/network barriers, no real Discord/provider calls. Evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/preview-worktree-ko9b2h64/`; negative control `preview-worktree-amd3ar58/`. Scoped Ruff clean except the three unchanged `bot.py` baseline findings.
- Root **approved deployment after validation**, including a fresh rollback backup and brief maintenance pause; current credentials/configuration and restart semantics must remain unchanged. Final combined suite, image build/synthetic acceptance and deployment remain pending. The alias-only installation below is already live; the image/debug/preview repairs are not.

## Start alias installed; combined source suite passed (2026-09-10)

- Exact source **`9ca15323a6e59dcceef289b61368f7267e79b404`** passed the complete isolated suite: **2,096 passed, zero failures/errors/skips**, 95.864s; the mandatory live-credential test was excluded. Python3.14.4, Git-archive source only, clean environment/disabled dotenv, isolated writable roots, private-read audit and loopback-only network namespace. Evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/9ca15323a6e5-uqrquzdz/{pytest.log,results.xml}`. Includes HD independence/one-request parser repair, loaded-runtime debug, and start alias; does not include the subsequent duplicate-preview fix in progress.
- Installed **only** the tested operator script at `/opt/maxwell/scripts/instance.py` under the instance operation lock, after confirming the existing script matched the expected two-line pre-alias baseline. Installed bytes match the tested snapshot; actual CLI help accepts `start`. All Compose container start epochs/restart counters stayed unchanged. Prior script: `/srv/maxwell-rollback/curie/start-alias-9ca1532/instance.py` (root-only).
- `start`/`up` are now interchangeable in the operational command. **Application image remains `99fd7fa`**; source fixes for image billing/debug are not yet deployed and `hd_image` remains temporarily disabled. No configuration, persona, credentials or private memory was changed by script installation.
- Artifact verification for the billed-image window found one retained 73,836-byte Pollinations image at 01:32:04.879 UTC and no HD persistence event. The HD tool retains no successful-but-unparsed response body, so the lost paid output cannot be recovered from its local storage. Root's two uploaded copies have identical SHA-256; the screenshot confirms attachment plus final-link preview. The final reply omitted the Discord URL's signed query, so preview suppression must recognize the same Discord attachment resource rather than rely on exact full-URL equality.

## Lifecycle start alias — implementation checkpoint (2026-09-10)

- Root requested systemctl-style spelling without changing restart scope. `scripts/instance.py <id> start` now takes the exact same path as `up`: ownership inventory and private operation lock, then `compose up -d --wait --wait-timeout 300`. The older spelling remains supported. `restart` is unchanged: bot/API only, not dependency recovery or a full stop/start cycle.
- **59 focused isolated operations tests passed**, including 10 new parameterized cases for CLI acceptance, identical health wait, lock handling, failed ownership, archive-argument rejection and unchanged restart command. Python3.14.4, synthetic/mocked operations only; Ruff clean. Evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/start-worktree-llrr22pr/`.
- Source and runbooks updated. Full combined validation and installation of the operator script at `/opt/maxwell/scripts/instance.py` are pending; no actual lifecycle operation was invoked for these tests.

## Duplicate image billing — traced and fixed in source (2026-09-10)

- Root's OpenRouter CSV identified two pairs of billed Gemini image requests at 01:30:40/49 UTC and 01:31:44/53 UTC. Sanitized production logs show HD attempt 1/2 reporting empty parsed content at **01:30:49.568358** and **01:31:53.329785**, immediately before each second billed request. Persisted tool traces show **one HD invocation per pair**, not two model tool calls. Both invocations returned errors and posted no HD image.
- The parser ignored the supported OpenRouter `message.images[].image_url.url` response field; [OpenRouter's SDK schema/test](https://github.com/OpenRouterTeam/ai-sdk-provider/commit/ac62ee7d11f0ad491700186b678c0664d2eb5229) explicitly permits empty content alongside images. Retained logs prove internal retries, not the unrecorded raw response body. Source now handles those fields and equivalent content image-url parts, retains legacy inline data URIs, and permits **one generation POST per tool execution**, including ambiguous responses/timeouts/5xx. Errors warn about possible billing/no automatic repeat instead of inventing a safety refusal or encouraging another generation.
- **52 focused isolated tests passed** (49 HD, 3 Pollinations). Against `d02e67d`, 31 new-suite cases fail; 20 specifically reproduce a second POST. All HTTP/delivery/persistence mocked; Python3.14.4 and private-read/network barriers. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/hd-image-uncommitted-sxgdtd71/`; baseline `hd-image-uncommitted-e8bqp460/`. Scoped Ruff clean; complete combined suite pending.
- Separate display evidence: one successful Pollinations fallback completed at 01:32:05, then the final stored reply at **01:32:07.975223 UTC** reused that already-delivered image's URL. That can render the same asset again as a Discord preview; it is not evidence of another paid generation. No Discord message fetch or presentation changes were made. This patch does not deduplicate independently requested tool invocations.
- **Not deployed.** `hd_image` remains temporarily disabled in the running `99fd7fa` image. No paid generation, Discord test message, memory write or runtime restart was used for diagnosis or regression testing.

## Loaded-runtime debug — implementation checkpoint (2026-09-10)

- `!debug` now reports loaded primary/fallback model and secret-safe provider hostnames from the running provider object, before any completion is needed. Measured replies remain a separate response-owned section and may represent a different fallback/override model. The complete report uses fenced code blocks, without a meaningless unmeasured footer. No configuration reread or inference/probe is performed.
- **64 focused isolated tests passed**, including 9 new cases for no history, stale file config, different measured endpoints, credential-safe host labels, fencing/chunking and unchanged measured ownership. Python3.14.4, source-only/private-read/network isolation; evidence `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/debug-worktree-h3rzr1_l/`. Scoped observability/test Ruff passes; `bot.py` retains exactly its three pre-existing lint findings. Not deployed; combined full suite is pending.
- Runbook now explicitly distinguishes full-stack `up` after `stop` from bot/API-only `restart`, and explains historical log replay. No lifecycle semantics or running services changed by that documentation.

## HD image separation — implementation checkpoint (2026-09-10)

- Root requested that HD generation remain available but never borrow the main chat endpoint/key. `hd_image` now resolves only dedicated `GEMINI_IMAGE_*` settings; missing base returns a configuration error before downloads/HTTP. Blank dedicated key sends no Authorization header, preserving explicit keyless gateways. Pollinations and tool registration are unchanged. Native GPT Images routes are not implemented by the existing chat-completions adapter.
- **19 focused isolated tests passed** (16 new HD cases, 3 existing Pollinations); 10 new cases fail on the prior source. Mocked HTTP/delivery only, Python3.14.4, private-read/network barriers. Scoped Ruff passes. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/hd-image-uncommitted-5vp_fsqf/`. Full combined-suite verification and deployment remain pending.
- Immediate authorized containment: `hd_image` added to the persisted runtime disabled-tools list through the authenticated API; `image_generator` and every other control preserved. The earlier control file is backed up under `/srv/maxwell-rollback/curie/image-optout-*/`. This is temporary until safe source is deployed; the tool is not being removed.
- Current image remains `99fd7fa`. The reported `ollama` DNS failures were real but before the current bot/API startup (01:55:50 UTC); Ollama started at 01:50:51 UTC. Both containers now resolve its name and pass actual finite/nonzero 1024-dimensional embedding probes. No DNS or embedding-breaker errors in the current process epoch. Latest read-only counts: 872 total, 838 trusted embeddings, 34 eligible unembedded, zero stale/invalid; new embeddings progressed but historical backlog remains. No re-embedding, model switch, service restart or image generation was performed in this investigation.

## Current deployment — TTS repair (2026-09-10)

**Curie is running `maxwell-app:99fd7fa`**, frozen source `99fd7fa4bcd4083653cbd29402840edc7b4bc97f`, Python3.14.4. Web remains `maxwell-web:01e8cbb`; private engine, loopback dashboard and original identity are unchanged. This release also deploys the primary request options below, without changing chat model/routing/reasoning settings.

- `TtsTool.execute` owns a per-call `/tmp/tts_*` directory for WAV/OGG synthesis, conversion and delivery. The application filesystem stays read-only; success, provider/send failure and awaited cancellation clean scratch. Provider order, voices, languages and cooldown are unchanged.
- **60 focused isolated tests passed**, including 12 new lifecycle/path cases. Both read-only-write and partial-provider-cleanup regressions fail against the previous source. Exact `99fd7fa`: **2028 complete-suite tests passed, zero skipped, one mandatory deselection**, 95.02s, exit0, Python3.14.4 with source-only archive, isolated roots, disabled dotenv, private-read guard and loopback-only network namespace. Evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/99fd7fa4bcd4-zh4_rdh1/`. Scoped Ruff passes.
- Actual old image reproduced EROFS under read-only `/app`. Actual new image passed the same full TTS path using real local eSpeak PCM, then passed **real NVIDIA Riva synthesis**: 135168 PCM bytes converted to 6417-byte non-silent Opus, 1.539s. Both used absolute scratch and removed temporary directories. Delivery was intercepted: **no test Discord send**. Actual `MaxwellBot()` construction/closure also passed in a credential-free, networkless, read-only container.
- The key root added was in the development checkout `.env`, not the active private config. Only `NVIDIA_API_KEY` was transferred into `/srv/maxwell/curie/config/bot.env` (mode0600, service-user owned). All other parsed settings match the saved prior config; current chat model remains `google/gemini-3.7-flash`. Environment changes require bot/API restart; editing the development `.env` does not configure the container.
- Production was quiesced once, backed up, and recreated with the verified image. Same Discord ID, fresh post-restart snapshot, provider initialization, zero restarts and zero error-level startup lines verified. Dashboard original credentials authenticate; unauthenticated API401 and private config404 remain enforced. Screen is retained and its log-following lock remains held. Backup had 672 vectors; running instance subsequently had 683/683 embedded with zero pending—no baseline-count rollback occurred.
- Fresh validated state backup: `/srv/maxwell-backups/curie/pre-tts-99fd7fa.tar`, including the staged NVIDIA key. SQLite backup was read successfully with its WAL. Original pre-key `bot.env`, old `deploy.env` and source archive are under root-only `/srv/maxwell-rollback/curie/tts-99fd7fa/`; old app image `3229a84` remains available. No state migration/schema change was introduced. Diagnostics (redacted, synthetic audio metadata) are under `/home/codexy/.cache/maxwell-tts-fix.F9BDBX/`.

## Primary request options — included in current image

- Added `OLLAMA_EXTRA_BODY` and `OLLAMA_EXTRA_HEADERS` as strict JSON-object configuration for the main client's primary endpoint. Enables OpenRouter `provider.only` and explicit `reasoning.effort` without changing account/workspace-wide routing. Runtime fields and explicit reasoning-disable calls retain precedence; API-key Authorization wins case-insensitively.
- Primary options are not sent to fallback/vision or separately constructed background clients. Shared main-client background calls inherit them; nested request bodies are independently copied for each attempt. Malformed configuration fails closed without printing its contents; existing lenient X override parsing is unchanged.
- Verified source `ac5b3a668fc8e38d9b0cd11164d3ea6226afc743`: **327 focused provider/startup tests passed**, then **2016 complete-suite tests passed, zero skipped, one mandatory deselection**, 95.14s, exit0. Python3.14.4, source-only Git archives, disabled dotenv, clean synthetic roots, private-read guard and loopback-only network namespaces. Includes 43 new option/config/wiring regressions with mocked provider transports; scoped Ruff passes. Full evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/ac5b3a668fc8-ginqmo95/{revision.txt,pytest.log,results.xml}`. No paid chat completion was used as acceptance.
- This feature was source-only at its implementation checkpoint and is now deployed in `99fd7fa`. Main/autonomy reasoning remains not explicitly disabled with no requested effort; auxiliary calls request reasoning disabled. No provider pin or model switch was applied.
- Configuration examples and precedence: [configuration guide](CONFIGURATION.md#custom-request-options-and-openrouter-routing) and `.env.example`.

## Initial rootless deployment — 2026-09-09 (historical acceptance)

**Curie is running in her private rootless Docker stack.** Real Discord login matches the original identity; the existing chat provider initialized successfully. The authenticated dashboard reports online from a fresh Discord snapshot. First successful live check: 1174/1174 trusted embeddings, zero pending, zero container restarts and zero error-level startup log lines. Bot activation and memory storage remain enabled. Counts naturally change during operation.

- Application: `maxwell-app:3229a84`, frozen source `3229a846e001466e7ad8cbece7682a301a04f575`, Python **3.14.4**, clean build provenance verified inside the running container.
- Web: unchanged tested `maxwell-web:01e8cbb`. Later documentation-only commits do not alter either frozen artifact.
- Account/engine/project: `maxwell-curie` UID1003, `/run/user/1003/docker.sock`, `maxwell-curie`. Per-user Docker enabled with lingering.
- Operational source: root-owned `/opt/maxwell`; private configuration/state under `/srv/maxwell/curie`.
- Dashboard: **http://localhost:8081/admin/**, original administrator credentials retained. Only loopback is published; no host API/Ollama port. Forward 8081 over SSH when remote.
- Existing `152732.dame_curie` Screen/window 0 is retained, attached displays untouched. It follows Compose logs under the original advisory lock. **Ctrl-c stops the log follower, not the bot.** Use [the current Screen runbook](SCREEN_WORKFLOW.md) for lifecycle commands.
- The disposable `acceptance` stack was removed after successful acceptance; its engine is stopped and lingering disabled. Private test state, images and model cache remain for repeatability. Its shutdown left Curie's container IDs/start times/restart counts unchanged. Unrelated rootful `cli-proxy-api` remains running and was not modified.

## Verified evidence

| Area | Evidence |
| --- | --- |
| Complete regression | Exact `3229a84`: **1973 passed, zero skipped, one mandatory live-credential test deselected**, 94.99s, exit 0. Python 3.14.4, fresh source-only Git archive, isolated home/data, private-read barrier, fresh user/network namespace with only loopback. Actual Chromium/Caddy/API/dashboard cases ran. |
| Bot startup | First `01e8cbb` launch exposed `/app/data` EROFS in `PluginManager`, before any Discord login. Stopped rather than weakening the read-only filesystem. `3229a84` passes the configured data directory explicitly. Two real constructor regressions fail before/pass after; complete constructor/close also passed in both actual read-only, networkless engines with synthetic state and all 79 tools. Corrected production login then succeeded. |
| Packaging | Actual rootless app/web/shell/site builds, optional dependency imports, accurate frozen build metadata. Final app also synthesized/decoded local audio and rendered an 800×600 Chromium screenshot using writable scratch under read-only/no-capabilities/no-network restrictions. No voice-call side effect was tested. |
| Dashboard | Actual local Caddy/API/Chromium login, prompt save/reload and desktop/mobile layout pass. Both Docker dashboards returned HTML200, unauthenticated401, authenticated status200 and private config404. Production authentication and fresh online status pass with unchanged real credentials. |
| Private Ollama | Separate per-engine model volumes, one-shot registry downloader, private internal runtime network. Both pulled `qwen3-embedding:0.6b` using pinned Ollama 0.33.3 and returned real valid 1024-dimensional vectors. No host Ollama/API publishing. |
| Semantic recall and recovery | Both instances returned their own synthetic answer at top1; peer fact absent. Cold embedding 1051/1547ms, warm query 139/111ms. Physical Curie Ollama stop retained a raw NULL-vector fact; real cooldown/restart/backfill recovered it. Synthetic final counts Curie4/4, acceptance3/3 trusted, nothing pending. |
| Backend-change safety | Backend-tagged vectors/cache, strict validation, stale/legacy exclusion without raw-fact deletion, snapshot/CAS writes and bounded resumable backfill. 178 focused cases plus independent negative/entity/cache-deadline repros and full suite. |
| Generated backends | Actual UID10001 inherited-ACL writes, read-only source, private DNS and Caddy→API→backend routing. Source labels, immutable IDs and confined ownership paths checked. |
| Shell | 56 focused cases plus actual lazy build, exec, confined immutable-ID export and escaping-symlink denial. Verified 4GiB/2CPU/1024PIDs, no ports/devices, required tmpfs/caps/security/process/network contract. |
| Backup/restore and isolation | Real synthetic backup/down/empty-target restore: 12 files hash/owner-identical, including UID10001. Reapplied ACLs; restored dashboard/backend worked. Peer IDs/start times/health unchanged. Reverse peer shutdown later left production unchanged. Cross-user configuration/socket access denied. |
| Existing response features | Telemetry, retries, footers, debug/version and mention fixes retained and covered by the complete regression; actual frozen container build metadata verified. No test Discord message was sent to claim footer rendering. |

Full-suite evidence: `/home/codexy/.cache/maxwell-full-20260909.XKjuHF/3229a846e001-avhup6un/{revision.txt,pytest.log,results.xml}`. Earlier exact `01e8cbb` passed1971 tests, but that is historical evidence, not a substitute for the final rerun.

Mandatory deselection: `tests/test_tool_progress.py::test_streaming_tick_inserts_space_between_glued_deltas` directly reads deployment credentials and can contact a real provider. No other tests were excluded or skipped. No test Discord/email/X/CAPTCHA side effects were sent. Production startup was the final rollout, not a credential-bearing test fixture; normal configured activity may resume after login.

## Migration and rollback

1. The legacy Screen bot was confirmed stopped with no source-tree Python process. Original configuration/state were archived privately before migration; original files and the Python 3.13.5 venv remain in place.
2. The explicitly **blank** old site setting resolved to the checkout, not a safe public root. Migration used the canonical `public/bot` subtree—seven files—not the checkout, `.env`, Git, venv or memory. No registered/running generated backend or existing shell source was present.
3. Private staging was inspected for symlinks, dotenv and special files. Target synthetic roots were preserved separately, then replaced only with the target engine stopped. Original Discord/chat/admin credentials, chat model, identity, feature controls and personality were retained. Only deployment paths, loopback origin and the private embedding backend were adjusted; duplicate infrastructure keys were replaced, not appended blindly.
4. **All 1171 original raw rows/columns, personality, controls, server prompts and public bytes were verified preserved.** Explicit private-Ollama backfill embedded1171/1171, zero failures/stale/invalid/pending; raw columns were rechecked after backfill. The bot subsequently reported1174/1174 at its first healthy check.

Retained private artifacts:

- `/srv/maxwell-rollback/curie/pre-cutover/legacy-state.tar`: original configuration/state, root-only (0600).
- `/srv/maxwell-rollback/curie/pre-cutover/source-96b4803.tar`: matching starting source, root-only.
- `/srv/maxwell-rollback/curie/pre-cutover/curie-deploy-fixed.env`: corrected image/deployment settings; normal state archives do not include `deploy.env`.
- `/srv/maxwell-backups/curie/pre-login-01e8cbb.tar`: ready 1171-row pre-login state, 0600, credentials included. The filename records when it was taken, **not** the image to relaunch; use the corrected application image `3229a84`.
- `/srv/maxwell-backups/curie/synthetic-final.tar`: tested synthetic restore archive. Original synthetic roots and private migration staging remain outside the production instance.

See [backup/restore instructions](DOCKER.md#backup-and-restore). Restore requires the same instance ID, no owned containers and empty config/data/sites/shell roots. It leaves the instance stopped. Reapply mapped UID ACLs before generated-backend startup; model weights are separate cache, not state-backup contents. No automatic or silently parallel host-native fallback is installed.

## Boundaries and remaining operator choices

- Public generated-site links remain loopback-only until root supplies an approved TLS origin/reverse proxy. Do not expose the dashboard or Ollama to make links convenient.
- Physical users/engines/state provide identity isolation. An unchanged model tag with changed upstream weights is **not** detected by endpoint/model/dimension/derivation fingerprints. The actual selected model and pinned server were live-tested; no unsupported minimum-version claim is inferred.
- The shell workspace is shared within one identity, not isolated per Discord user. Networked tools retain outbound access; this is not universal network isolation.
- Existing optional account/feature settings were preserved, not invented. Provider initialization is a successful `/models` probe; no synthetic paid chat completion or Discord/email/X/CAPTCHA action was used as acceptance.
- Scoped new-code checks pass. `bot.py` retains three unchanged baseline Ruff findings (unused import/local and an unretained plugin-dispatch task), independently reproduced on `01e8cbb`; no linter rule was relaxed and no unrelated refactor was added.
- Historical native scripts and portable HTML guides are reference/rollback material, not instructions to launch a second bot. Use the current [Screen runbook](SCREEN_WORKFLOW.md) and [rootless operating reference](DOCKER.md).

## Rollout commits

| Commit | Verified slice |
| --- | --- |
| `9659a61` | Replace stale audit with operating contract and current rollout ledger |
| `152bda5` | Provision private rootless engines and own embedding lifecycle |
| `112b438` | Reject shell resource/process/network drift before reuse/export |
| `55417f6` | Isolate vector spaces and add validated resumable backfill |
| `01e8cbb` | Package healthy dashboards, compatible pinned Ollama and frozen provenance |
| `2c0fccc` | Record complete regression and real restored-service acceptance |
| `3229a84` | Fix actual read-only bot startup; add real constructor regressions |

Starting source was `96b4803` on `pr/fixing_number_max_tool_calls`. Historical detail remains in Git. No push, amend, rebase or unrelated supervisor migration was performed.
